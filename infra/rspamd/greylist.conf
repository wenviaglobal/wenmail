# Greylisting — delays first-time senders by 5 minutes
# Kills most spam bots (they don't retry)
enabled = true;
expire = 86400;  # Remember sender for 24 hours after first pass
timeout = 300;   # 5 minute greylist delay
