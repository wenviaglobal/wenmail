# Spam score thresholds
reject = 15;          # Hard reject — definitely spam
add_header = 6;       # Add X-Spam header, deliver to Junk
greylist = 4;         # Greylist suspicious messages
rewrite_subject = 8;  # Rewrite subject with [SPAM] prefix
